version: "3"

services:
  kronic:
    image: mshade/kronic
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - $HOME/.kube/kronic.yaml:/root/.kube/config
    ports:
      - 5000:5000
    networks:
      - k3d-kronic

networks:
  k3d-kronic:
    external: true
